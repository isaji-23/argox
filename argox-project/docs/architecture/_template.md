# ADR-NNNN: <decision title>

- **Status:** proposed | accepted | superseded by ADR-MMMM
- **Date:** YYYY-MM-DD
- **Ticket:** TICKET-NN

## Context

What problem forced this decision? What constraints applied (framework
behaviour, OTel semconv, fail-open semantics, demo deadline)? Keep it factual.

## Decision

The decision taken, in present tense. Include the concrete shape (signature,
contract, data flow) so a future contributor can extend it consistently.

## Triggers for the next refactor

The conditions under which this decision should be revisited. Be specific —
"when a second optional hook lands" beats "when it gets complex".

## What stays out of scope

What this decision deliberately does NOT cover, so the boundary is explicit.
